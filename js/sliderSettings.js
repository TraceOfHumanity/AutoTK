$(document).ready(function(){
  $('.brand__slider').slick({
    slidesToShow: 5,
    slidesToScroll: 1,
    infinite: true,
    // initialSlide: 3,
    focusOnSelect: true,
    // centerMode: true,
  });
  $('.reviews__slider').slick({
    slidesToShow: 3,
    slidesToScroll: 1,
    infinite: true,
    // initialSlide: 3,
    focusOnSelect: true,
    // centerMode: true,
  });
  $('.news__slider').slick({
    slidesToShow: 3,
    slidesToScroll: 1,
    infinite: true,
    // initialSlide: 3,
    focusOnSelect: true,
    // centerMode: true,
  });
});